pandas>=2.0
numpy>=1.26
google-cloud-bigquery>=3.20
google-cloud-aiplatform>=1.60
ortools>=9.9
streamlit>=1.35
python-dotenv
networkx>=3.4
folium>=0.20
streamlit-folium>=0.20
requests>=2.31

# Optional, only if you later swap the free OpenStreetMap basemap for real Myanmar OSM building
# footprints. geopandas can be tricky to install on Windows (needs GDAL) - skip unless needed.
# geopandas>=0.14
# shapely>=2.0

# Run on a CUDA GPU (e.g. Google Colab) for the RAPIDS acceleration benchmark:
# cudf-cu12  (installed separately, see benchmark/README.md)
